# Operator guide

> Click-by-click instructions for non-engineers. Filled in across Phase 2 (CLI), Phase 3 (web), and Phase 4 (bot). This is a placeholder so future-you knows where it goes.

## Sections to fill

1. First-time setup — `tent init`
2. Adding a server (CLI / web / bot variants)
3. Adding a site (CLI / web / bot variants)
4. Common operations
   - Redeploying a site after code changes
   - Rotating a secret
   - Setting up a backup destination
   - Destroying a site (and recovering one)
5. When something goes wrong — `tent doctor` and how to read its output
6. Glossary (server vs site vs template vs job vs tunnel)
