{
  "name": "wordpress",
  "version": "1.0.0",
  "description": "WordPress + MariaDB via Docker Compose. Note: destroy removes both the wp-content host directory and the db volume — there is no automated backup until Phase 6. Don't destroy a real WP site without an external backup.",
  "variables": {
    "site_title": {
      "type": "string",
      "description": "Browser-tab title used during the WordPress installer.",
      "default": "tent wordpress site",
      "optional": false
    },
    "admin_email": {
      "type": "string",
      "description": "Email address that will be the WordPress admin during install.",
      "optional": false
    },
    "db_password": {
      "type": "string",
      "description": "Password for the wordpress mariadb user (root is randomly generated).",
      "optional": false,
      "secret": true
    }
  },
  "requires": {
    "docker": true,
    "postgres": false,
    "nodejs": false
  },
  "ports": {
    "internal": 80
  },
  "healthCheckPath": "/wp-login.php"
}
